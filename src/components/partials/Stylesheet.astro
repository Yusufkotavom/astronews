---
// Optimized Stylesheet Component with Font Optimization
---

<!-- Optimized font loading -->
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

<!-- Load Google Fonts with optimization -->
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />

<style>
  /* Optimized font fallback */
  .font-optimized {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
    font-display: swap;
  }

  /* Performance optimizations */
  * {
    box-sizing: border-box;
  }

  /* Optimized image loading */
  img {
    max-width: 100%;
    height: auto;
  }

  /* Optimized focus styles */
  *:focus {
    outline: 2px solid #3b82f6;
    outline-offset: 2px;
  }

  /* Reduced motion for accessibility */
  @media (prefers-reduced-motion: reduce) {
    *, *::before, *::after {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
    }
  }
</style>

<!-- Tailwind CSS is handled by @astrojs/tailwind integration -->
<!-- Flowbite is now handled by the Tailwind plugin for better performance -->